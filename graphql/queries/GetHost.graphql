query GetHost {
	host {
		title
		slogan
		contacts
		owner {
			id
			name
			slug
			avatar {
				url
			}
		}
		logo {
			url
		}
		banner {
			url
		}
		authBanner {
			url
		}
		description
		firstSettings
	}
}
